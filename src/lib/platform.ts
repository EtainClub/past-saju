/**
 * 지금 돌고 있는 번들이 토스 미니앱용인지.
 *
 * 빌드 시점에 박히는 값이다. 토스 번들은 `pnpm build:toss` 로 따로 만들고
 * 그 산출물은 토스 안에서만 열리므로, 실행 중에 환경을 알아맞힐 필요가 없다.
 * 런타임 감지(userAgent 추측 같은 것)는 틀릴 여지가 있지만 이건 틀릴 수 없다.
 *
 * ## 왜 이 값이 필요한가
 *
 * 미니앱 WebView 안에서는 **구글 OAuth 가 성립하지 않는다.** 팝업이 토스의
 * 인앱 뷰로 가로채여 인증 파라미터가 끊기고, 구글은 정책상 임베디드
 * WebView 로그인을 막으며, 앱 출처(minion.toss.im)와 인증 도메인이 달라
 * 브라우저의 저장소 분리에도 걸린다. 셋 중 하나만 있어도 막히는데 셋 다 있다.
 *
 * 그래서 구글 연동에 매달린 기능 — 저장과 보관함 — 을 미니앱에서는 **아예
 * 보여 주지 않는다.** 눌리지 않는 버튼을 두는 것보다 없는 게 낫다.
 *
 * 사주 읽기는 익명으로 통과하므로(`/api/reading/saved` 만 연동을 요구한다)
 * 미니앱에서도 제품의 본체는 그대로 동작한다.
 *
 * 토스 로그인(`TossAuth.login`)으로 신원을 받아 오면 이 제약이 풀린다.
 * 그때 이 값을 쓰는 자리들을 되돌리면 된다.
 */
export const IS_TOSS_APP = process.env.NEXT_PUBLIC_TOSS_APP === "1";
